{
  "name": "@f/has-class",
  "description": "Check whether or not an element has a class",
  "repository": "git://github.com/micro-js/has-class.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "hihat": "^2.6.4",
    "tap-dev-tool": "^1.3.0",
    "tape": "^4.4.0"
  }
}
